
OCaml library that wraps the Cassandra interface generated by thrift.

Compiling
=========

(1) set the THRIFT env. var to point to the thrift executable

(2) run

    omake

Example:

   THRIFT=/tmp/thrift/compiler/cpp/thrift omake --verbose

Usage
=====
See cassandra.mli.
